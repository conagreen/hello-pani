# 선택 확장 — Prometheus + Grafana로 부하 진행을 실시간 시각화한다.
# 기본 데모 경로는 docker-compose.yml + ./gradlew bootRun이고, 이 파일은 옵션이다.
#
# 사용법:
#   docker compose -f docker-compose.yml -f docker-compose.observability.yml up -d
#   ./gradlew bootRun                     # 호스트에서 실행
#   ./scripts/test-load.sh                # 다른 셸에서 부하
#   open http://localhost:3000            # admin / admin (첫 로그인 시 변경 가능)
#
# Linux Docker에서는 host.docker.internal 매핑을 위해 host-gateway가 필요하다.

services:
  # base docker-compose.yml의 mysql 서비스에 perf_schema 권한 init script만 추가한다.
  # 처음 빈 볼륨으로 시작하는 경우 자동 적용되며, 기존 볼륨에는 docker exec 으로 한 번 실행해야 한다.
  mysql:
    volumes:
      - ./observability/mysql-init:/docker-entrypoint-initdb.d:ro

  prometheus:
    image: prom/prometheus:v2.55.0
    ports:
      - "9090:9090"
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=2h

  grafana:
    image: grafana/grafana:11.3.0
    ports:
      - "3000:3000"
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
    volumes:
      - ./observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus

  redis-exporter:
    image: oliver006/redis_exporter:v1.66.0
    ports:
      - "9121:9121"
    environment:
      REDIS_ADDR: "redis://redis:6379"
    depends_on:
      - redis

  mysql-exporter:
    image: prom/mysqld-exporter:v0.16.0
    ports:
      - "9104:9104"
    command:
      - --config.my-cnf=/etc/.my.cnf
      - --collect.global_status
      - --collect.global_variables
      - --collect.info_schema.processlist
      - --collect.info_schema.innodb_metrics
      # SQL digest별 통계: 어떤 패턴이 얼마나 도는지 정량화. PreparedStatement 파라미터는 자동 정규화.
      - --collect.perf_schema.eventsstatementssum
    volumes:
      - ./observability/mysql-exporter.cnf:/etc/.my.cnf:ro
    depends_on:
      - mysql
